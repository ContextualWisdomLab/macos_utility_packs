# Claude Code project instructions

Read [`AGENTS.md`](AGENTS.md) and the generated shared guidance before changing
this repository. Use `scripts/test` before reporting a change, preserve managed
configuration boundaries, and never print or commit authentication tokens.

The repository is a local macOS bootstrap tool; do not introduce a service,
database, UI, or cloud dependency unless a separate requirement establishes an
independent operational boundary.
